# Copy this file to the PROJECT ROOT as .env and fill values (DON'T commit .env with real secrets)
# Example: cp android/fastlane/.env.example .env
# Recommended env var names used by Fastfile:
FIREBASE_APP_ID=1:766832833441:android:40e6a427aade9ea5fd91e0
# The Firebase CLI token used by `firebase appdistribution:distribute`.
# You can get it via `firebase login:ci` when using the Firebase CLI locally.
FIREBASE_CLI_TOKEN=your_firebase_cli_token_here

# alternative names supported by the Fastfile (optional):
# ANDROID_FIREBASE_APP_ID=
# FIREBASE_TOKEN=
